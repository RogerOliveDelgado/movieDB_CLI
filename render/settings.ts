import { chalkObject } from "../interfaces/chalkSettings";
import chalk from "chalk";

export const chalkSettings: chalkObject = {
  white: chalk.white,
  bold: chalk.bold,
  blue: chalk.blue,
  yellow: chalk.yellow,
  magenta: chalk.magenta,
  breakLine: "\n",
  tab: "\t",
};
